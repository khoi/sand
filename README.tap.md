# sand Homebrew Tap

This tap provides a Homebrew formula for the `sand` CLI.

## Install

```
brew tap khoi/tap
brew install --HEAD sand
```

## Notes

- `sand` requires macOS 14+ and Tart in your PATH.
- Once a tagged release exists, update the formula to use the release tarball for a stable install.
